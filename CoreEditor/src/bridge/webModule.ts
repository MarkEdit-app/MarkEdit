/**
 * Module used to invoke web from native.
 *
 * Implementaions of WebModule should be wrappers to functions in modules.
 */

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface WebModule {}
